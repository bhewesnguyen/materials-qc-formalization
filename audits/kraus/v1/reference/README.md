# Optional convergence API probe

This auditor-authored source compiles at the accepted Kraus pin. It checks an
explicit Frobenius definition against Mathlib's named scoped norm and proves the
two scalar exponential limits used by the next assignment. It does not prove
the evolution estimate, matrix convergence, or any other new release endpoint.

After integrating the return, run from the repository root:

```sh
lake env lean audits/kraus/v1/reference/ConvergenceApiProbe.lean
```

The recorded command, source hash, stdout, stderr and four axiom reports are in
`../evidence/convergence-api-probe/`. The probe is optional proof guidance.
Any adapted declaration must be acknowledged and pass the normal release,
contract and axiom checks. Do not import an audit artifact as a release module.
